# Flutter / Dart
.dart_tool/
.packages
build/
.flutter-plugins
.flutter-plugins-dependencies
*.iml

# Generated by `just gen`. Committed in most frb
# projects; kept out here so a stale generation can never be reviewed as if it
# were hand-written bridge code.
lib/src/rust/

# Cargokit / rust_builder intermediate output
rust_builder/**/build/
