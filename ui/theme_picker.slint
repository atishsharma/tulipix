import { Theme } from "tokens.slint";
import { Surface, Rim, Skin } from "surface.slint";

export enum ThemeChoice { system, light, extra-dark, oled }

export component ThemePicker inherits Rectangle {
    in-out property <ThemeChoice> choice: ThemeChoice.system;
    callback changed(ThemeChoice);

    height: 40px;
    border-radius: Theme.radius-md;
    background: Surface.body(Theme.panel-2);
    border-color: Surface.hairline;
    border-width: 1px;

    HorizontalLayout {
        padding: 4px; spacing: 4px;
        for entry in [
            { label: "System", v: ThemeChoice.system },
            { label: "Light",  v: ThemeChoice.light },
            { label: "Dark", v: ThemeChoice.extra-dark },
            { label: "OLED", v: ThemeChoice.oled },
        ]: Rectangle {
            horizontal-stretch: 1;
            border-radius: Theme.radius-sm;
            background: root.choice == entry.v ? Theme.brand : transparent;
            touch := TouchArea {
                clicked => {
                    root.choice = entry.v;
                    root.changed(entry.v);
                }
            }
            Text {
                text: entry.label;
                color: root.choice == entry.v ? #ffffff : Theme.text-dim;
                font-size: 12px; font-weight: 600;
                horizontal-alignment: center; vertical-alignment: center;
            }
        }
    }
}
